✅ Tasks

1. Set up a terminal client of your choice (e.g., iTerm2 + zsh on Mac, Windows Terminal + PowerShell, or any other).
- I am using the built-in macOS Terminal with the zsh shell. zsh is the default command-line shell on modern Mac computers and provides useful features for navigating files, running Git commands, and managing development tasks.

- I have verified my terminal setup and will use it alongside Visual Studio Code and GitHub Desktop for development work.

2. Customize it if desired (e.g., themes, aliases, plugins).
- I customised my zsh terminal by adding useful aliases to make common commands faster. For example, I created shortcuts for navigating folders and checking Git status.

- Examples:
1. `alias gs="git status"` - Quickly check Git changes.
2. `alias ..="cd .."` - Move back one folder.
3. `alias ll="ls -la"` - Display all files in a directory.

- These customisations make the terminal easier to use and improve productivity by reducing repetitive commands.

3. Research basic terminal usage and write your reflections in terminal_knowledge.md:
- Which terminal client did you choose? Why?
- I chose the built-in macOS Terminal using the zsh shell because it is already installed on my MacBook and works well for running development commands, Git operations, and managing files.

- What customizations (if any) did you make?
- I customised my terminal by adding useful aliases such as `gs` for `git status` and `ll` for listing files in detail. These shortcuts help make common tasks faster.

- What was the most useful command you learned today?
- The most useful command I learned today was `git status` because it quickly shows the current state of my repository, including changed files and whether my work is ready to be committed.

4. Commit and push your changes to GitHub.
Done

